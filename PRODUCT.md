# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 주 사용자는 두 사람(커플)이며, 이 서비스는 그 둘만 쓰는 비공개 앱이다. 로그인만 제공되고 회원가입 UI가 없으며, 계정은 별도로 발급된다.
- 사용 상황은 세 가지다. 함께 다녀온 직후 사진·지도·짧은 글을 남길 때, 다음 나들이를 미리 계획할 때, 예전 기록을 다시 꺼내 볼 때.
- 외부 방문자나 일반 가입자는 대상이 아니다. 공개 서비스로의 확장은 현재 계획에 없다.

## Product Purpose

두 사람이 함께한 나들이와 여행을 남겨 두고 다시 들여다보는 개인 아카이브다. 성공은 기록이 꾸준히 쌓이고, 시간이 지나 다시 열었을 때 그날이 되살아나는 것이다. 여기에 둘만 아는 놀이 요소(나들이 퀴즈)가 얹혀, 기록이 상대에게 남기는 메시지 역할까지 한다.

## Positioning

일반 여행 기록 앱과 달리 이 제품은 두 사람만의 사적 맥락 위에 서 있다. 만난 날부터 세는 D-day, 상대가 풀도록 만들어 두는 나들이 퀴즈, 직접 만들어 올린 지도 이미지, 오래 걷기 좋은 신발 아카이브가 한 앱 안에 함께 있다. 공개 서비스가 그대로 가져갈 수 없는 조합이며, 이 사적 맥락을 지우고 일반화하는 방향은 후퇴다.

## Operating Context

- 디바이스별 내부 라우팅을 쓴다. PC는 `/main/*`, 모바일은 `/mobile/*`이며 각 경로가 자체 페이지 구현을 가진다.
- 나들이 기록(`trip`): 날짜, 유형, 이름, 로고 이미지, 지도 이미지 1~2장, 드라이브 URL, 이전/다음 버튼 문구, 그리고 퀴즈로 구성된다. 사진 원본은 외부 드라이브 링크로 연결한다.
- 여행 기록(`travel`): 1박 이상 여정을 날짜별로 기록한다.
- 달력(`calendar`): FullCalendar 기반 월간/주간 보기와 일정 CRUD, 일정 숨김을 제공한다.
- 신발 아카이브(`shoes`): 브랜드·모델·리뷰·영상 링크를 담은 카탈로그로, 오래 걷기 좋은 신발을 모아 둔다.
- 프로필: 이름과 프로필 이미지 수정, 비밀번호 변경(변경 전 비밀번호 확인 단계 포함).
- 홈 허브에서 위 영역으로 진입하며, 외부 필름 작업 링크(Choi's Film Art)로 나가는 진입점도 홈에 있다.
- 인터페이스 언어는 한국어다. 다국어 요구는 확인된 바 없다.

## Capabilities and Constraints

- 데이터 계약의 기준은 `docs/api_spec.json`이다. 인증, 나들이, 여행, 일정, 파일 자산, 사용자 프로필이 서버 API로 동작한다.
- 신발 카탈로그만 예외로 API가 아니라 코드 내 정적 데이터(`src/domains/shoes/data/shoes-data.ts`)다.
- 로그인 후 리다이렉트는 현재 디바이스 prefix 내부 경로만 허용한다.
- 회원가입 화면과 비로그인 열람 흐름은 존재하지 않는다.
- 이미지·지도는 업로드 자산 API(`/assets/files`)를 통해 올라간 파일을 참조한다.
- 미정으로 남은 제품 사실: 기록의 공유·내보내기·백업·삭제 정책, 사진 정렬 규칙, 지도 이미지 제작 방식, 다크 모드 채택 여부.

## Brand Commitments

- 제품명은 "서울 촌놈"(Seoul Chonnom, SLCN)이며, 홈 헤드라인은 "서울 촌놈 나들이 기록"이다.
- 로고는 별도로 제공된 `src/assets/img/SLCN.png` 하나만 사용한다. 다시 그리거나 색을 바꾸거나 텍스트 로고로 대체하지 않는다.
- 목소리는 한국어 구어체의 조용하고 다정한 톤이다. 홍보성 과장이나 서비스 마케팅 어조는 쓰지 않는다.

## Evidence on Hand

- 실제 기록과 사진은 운영 API에서 온다. 로컬에 고정된 콘텐츠가 아니다.
- 신발 카탈로그 실데이터는 `src/domains/shoes/data/shoes-data.ts`에 있다.
- 로고 자산은 `src/assets/img/SLCN.png`.
- 외부 필름 작업 링크(Choi's Film Art)가 홈에 있으나, 반드시 보존해야 할 제품 사실로는 확인되지 않았다.
- 존재하지 않으므로 지어내면 안 되는 것: 사용자 후기, 이용 지표, 언론 언급, 가격·라이선스, 외부 파트너, 공개 사용자 수.

## Product Principles

1. 기록이 주인공이다. UI는 사진과 글을 담는 액자이지 그 자체로 볼거리가 아니다.
2. 두 사람의 사적 맥락은 제품의 핵심이다. D-day와 퀴즈처럼 둘만의 요소를 일반적인 기능으로 희석하지 않는다.
3. 확정된 사실은 그대로 보존한다. D-day 기준일 2024-11-10, 나들이 퀴즈 기능, 정적 신발 아카이브.
4. `/main`과 `/mobile` 어느 쪽도 부산물이 아니다. 두 디바이스 경로를 동등한 완성도로 다룬다.
5. 사적인 공간이므로 공개 공유 성격의 기능은 명시적인 요청이 있기 전까지 추가하지 않는다.
