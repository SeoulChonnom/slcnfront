# Tests assert fully-qualified URLs, so they need a fixed absolute base that
# does not depend on a developer's untracked .env. Matches the value CI
# exports, so a local `pnpm test` and the CI run agree.
VITE_API_URL=http://localhost:8080/api
