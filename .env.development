# Same-origin on purpose: `pnpm dev` reaches the backend through the Vite
# proxy in vite.config.ts rather than calling it directly, so requests stay
# first-party and no dev-server port is hard-coded here. The proxy's target is
# where the real backend address lives.
VITE_API_URL=/api
